*def* : A **linear combination** of a set of vectors is a sum of scalar multiples of the vectors. More precisely, a vector $w$ is called a linear combination of the vectors $v_{1}, v_2 ... v_k$ such that

$$
w = \lambda_{1} v_{1} + \lambda_2 v_2 + ... + \lambda_k V_k
$$

